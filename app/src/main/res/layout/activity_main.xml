<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">


    <!-- SurfaceView for Filament rendering -->

    <!-- Button to Fetch Terrain -->

    <SurfaceView
        android:id="@+id/filamentSurfaceView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <Button
        android:id="@+id/fetchTerrainButton"
        android:layout_width="177dp"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="12dp"
        android:layout_marginBottom="666dp"
        android:text="@string/fetch_terrain" />

    <Button
        android:id="@+id/centerGpsButton"
        android:layout_width="167dp"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="224dp"
        android:layout_marginTop="93dp"
        android:layout_marginBottom="668dp"
        android:text="@string/center_gps" />

</RelativeLayout>
